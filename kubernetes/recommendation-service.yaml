apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: nexacart
  labels:
    app: recommendation-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
      protocol: TCP
  selector:
    app: recommendation-service
