apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: nexacart
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      name: http
      protocol: TCP
  selector:
    app: backend
