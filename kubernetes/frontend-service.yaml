apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: nexacart
  labels:
    app: frontend
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      name: http
      protocol: TCP
  selector:
    app: frontend
